const initialState = {
    checking: true,
    // uid,
    //name
}

export const authReducer = (state=initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}